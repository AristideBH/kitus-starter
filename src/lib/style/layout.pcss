@layer base {
	* {
		@apply border-border;
	}
	html {
		@apply scroll-smooth;
	}
	body {
		@apply flex min-h-dvh w-dvw flex-col overflow-x-clip bg-background text-foreground;
		& > div:first-of-type {
			@apply contents;
		}
		& main {
			/* @apply min-h-dvh; */
		}
	}
}
